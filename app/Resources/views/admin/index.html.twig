{% extends "base.html.twig" %}

{% block title %}| Creneau{% endblock %}

{% block h2 %}
    Liste des admins
{% endblock %}

{% block container %}
    {{ parent() }}


    {% for admin in admins %}
        {#<a href="{{ path('admin_show', { 'id': admin.id }) }}">{{ admin.id }}</a>#}
        <ul>
            <li>
               Nom utilisateur (email) : {{ admin.username }} | <a href="{{ path('admin_edit', { 'id': admin.id }) }}">edit</a>
            </li>
        </ul>
    {% endfor %}

    <ul>
        <li>
            <a href="{{ path('admin_new') }}">Ajouter un nouvel administrateur</a>
        </li>
    </ul>

{% endblock %}
